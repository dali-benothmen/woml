# WOML v1.0.0 Final Review

Review date: 2026-08-17  
Status: local V1R8 review passed; final remote artifact seal and product-owner
approval remain pending.

## Completed review

| Area | Evidence | Result |
| --- | --- | --- |
| Sensitive content | Automated scan of 1,048 tracked or candidate source files plus all 18 exact staged-package files and source maps | Passed; real-looking Slack workspace fixture and personal sample names were sanitized |
| JavaScript advisories | Live `bun audit --json` after lockfile remediation | Passed; zero advisories |
| Rust advisories | Live `cargo audit --json` against 342 locked dependencies | Passed; zero vulnerabilities and zero warnings |
| Dependency licenses | Cargo metadata and installed bundled-JavaScript manifests | Passed; all dependencies identified, permissive expressions only, no prohibited/unknown license |
| Legal metadata | Apache-2.0 `LICENSE`, packaged `NOTICE.md`, author, repository, homepage, issues, support and security routes | Passed |
| Package boundary | Exact staged package verifier | Passed; 18 files, no `.node`, no runtime data or credential-shaped content |
| Local package reproducibility | Two consecutive canonical `bun run pack` executions | Passed; both produced `4cc871e1c4ea8484ed12b102bea479e1fba9b95c554b650b8e04996f1307250f` |
| Network defaults | Loopback public/admin defaults, non-loopback admin rejection, container public/admin separation | Passed |
| Secrets and tokens | OS keychain restriction, mounted-file permission checks, constant-time admin capability checks, redaction tests | Passed |
| Runtime data | `.woml`, databases, archives, logs, environment files, and native binaries ignored and excluded from package | Passed |
| Security remediations | AJV 8.20.0, `reqwest` 0.12.28, `tokio-postgres` 0.7.18, `postgres-protocol` 0.6.12 | Rust workspace check and both live advisory audits passed |
| Ongoing advisory detection | Weekly/manual and dependency-PR GitHub workflow plus release-time audits | Passed; read-only permissions and exact audit tool version |

The repeatable local gate is:

```bash
bun run test:final-review
```

The dependency finding and remediation record is
[v1.0.0-dependency-review.md](v1.0.0-dependency-review.md). User-facing release
behavior and limitations are in
[v1.0.0-release-notes.md](v1.0.0-release-notes.md).

## Final seal still required

The exact release commit and cross-platform artifact checksums cannot be
truthfully frozen while these V1R7/V1R8 changes are uncommitted and the first
non-publishing GitHub matrix has not completed. Before V1R9:

1. commit the complete candidate and confirm a clean working tree;
2. run `.github/workflows/release.yml` with `publish_to_npm=false` from that
   exact commit;
3. require all eight native jobs, Docker restart smoke, collection, and final
   verification to pass;
4. record the source commit and collected artifact SHA-256 manifest here;
5. verify GitHub private vulnerability reporting is enabled for the repository;
6. rerun `bun audit --json`, `cargo audit --json`, and
   `bun run test:final-review`; and
7. obtain the product owner's explicit approval of that immutable candidate.

No npm publication or GitHub release is authorized by this review document.

## Approval record

| Field | Value |
| --- | --- |
| Source commit | Pending final candidate commit |
| Local staged-content digest | `24baa8bc128ab02d1269f432c4a93d4dd76fccfc38d93f3066804e0ee9de784b` (provisional) |
| Local main-package archive | `4cc871e1c4ea8484ed12b102bea479e1fba9b95c554b650b8e04996f1307250f` (provisional) |
| Non-publishing matrix | Pending |
| Collected artifact manifest | Pending |
| GitHub private vulnerability reporting | Owner verification pending |
| Product-owner approval | Pending exact candidate |
| npm publication | Not authorized; V1R9 only |
