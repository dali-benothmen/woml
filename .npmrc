# Publish scoped packages as public
access=public

# Use Bun as the preferred package manager for installs
package-manager=bun

# Disable package-lock.json generation (Bun manages lockfiles)
package-lock=false

# Strict SSL for npm registry
strict-ssl=true

# Default registry
registry=https://registry.npmjs.org/
