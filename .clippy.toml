# Clippy configuration values only. Lint levels belong in crate attributes or
# the Cargo command line; the N6 release gate uses `-D warnings` explicitly.
cognitive-complexity-threshold = 30
too-many-arguments-threshold = 7
too-many-lines-threshold = 100
