{
  "schemaVersion": 7,
  "workflowId": "event-trigger-contract",
  "triggers": [
    {
      "id": "orderCreated",
      "handler": "trigger.event",
      "config": {
        "kind": "object",
        "fields": {
          "name": { "kind": "literal", "value": "order.created" },
          "secret": { "kind": "secretReference", "name": "EVENT_CONTROL_TOKEN" },
          "schema": {
            "kind": "literal",
            "value": {
              "type": "object",
              "required": ["orderId"],
              "properties": {
                "orderId": { "type": "string" }
              }
            }
          }
        }
      }
    }
  ],
  "graph": {
    "entryNodeIds": ["capture"],
    "nodes": [
      {
        "id": "capture",
        "handler": "runtime.script",
        "inputs": {
          "kind": "object",
          "fields": {
            "source": { "kind": "literal", "value": "\n        return context.trigger;\n      " }
          }
        }
      }
    ],
    "edges": []
  }
}
